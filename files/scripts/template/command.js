const { langHandler } = require('../../files/translations/langHandler.js');

const { MessageEmbed } = require('discord.js');
const ee = require('../../config/embed.json');

module.exports = {
	name: "",
	category: "",
	aliases: [],
	usage: "",
	adminOnly: false,
	whitelistOnly: false,
	nsfw: false,
	run: async (client, message, args) => {
		const LANGUAGE = langHandler(message).


	}
};
